const initialState = {
    id: '',
    name: '',
    cards: []
}

const cardSchema = {
    id: '',
    questionText: '',
    answerText: '',
    correct: false,
}

export function decks(state=initialState, action) {
    // switch(action.type) {
    //     case ''
    // }
}
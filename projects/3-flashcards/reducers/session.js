const initialState = {
    active: true,
    deck: 'xy_3213223',
    correctAnswer: {
        xy_3213223: true
    }
}

const cardSchema = {
}

export function decks(state=initialState, action) {
    switch(action.type) {
        
    }
}